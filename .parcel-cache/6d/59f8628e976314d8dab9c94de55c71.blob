window.dom = {
    create(string){
        const container = document.createElement("template")
        //trim 去除两边空格
        container.innerHTML = string.trim()
        return container.content.firstChild
    },
    after(node, node2){
        //找到这个节点的爸爸,调用爸爸的insertBefore方法， 
        node.parentNode.insertBefore(node2, node.nextSibling)
    }
};